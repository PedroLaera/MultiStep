import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    height: 56,
    width: "100%",
    top: 50,
    backgroundColor: "#460399ff",
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 12,
  },
  title: {
    color: "#FFF",
    fontSize: 16,
  },

});
